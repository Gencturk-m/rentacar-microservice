# for read from local file
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: file:///D:/TurkcellBootcamp/microserviceRentACar/config-server-t1
      profile: dev # or prod or anything
  profiles:
    active: native
server:
  port: 8888